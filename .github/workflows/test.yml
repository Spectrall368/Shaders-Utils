name: Build and test

permissions:
  checks: write

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Checkout MCreator repository
        uses: actions/checkout@v4
        with:
          repository: MCreator/MCreator
          ref: master
          path: mcreator
      - name: Checkout 1.17.1 Generator
        uses: actions/checkout@v4
        with:
          repository: Spectrall368/Generator-1.17.1
          ref: 2025.2
          path: generator-1.17.1
      - name: Checkout 1.18.2 Generator
        uses: actions/checkout@v4
        with:
          repository: Spectrall368/Generator-1.18.2
          ref: 2025.2
          path: generator-1.18.2
      - name: Checkout 1.19.2 Generator
        uses: actions/checkout@v4
        with:
          repository: Spectrall368/Generator-1.19.2
          ref: 2025.2
          path: generator-1.19.2
      - name: Checkout 1.19.4 Generator
        uses: actions/checkout@v4
        with:
          repository: Spectrall368/Generator-1.19.4
          ref: 2025.2
          path: generator-1.19.4
      - name: Checkout 1.20.1 Generator
        uses: actions/checkout@v4
        with:
          repository: MCreator/Generator-Forge-1.20.1
          ref: master
          path: generator-1.20.1
      - name: Checkout 1.20.4 Generator
        uses: actions/checkout@v4
        with:
          repository: Spectrall368/Generator-1.20.4
          ref: 2025.2
          path: generator-1.20.4
      - name: Checkout 1.20.6 Generator
        uses: actions/checkout@v4
        with:
          repository: Spectrall368/Generator-1.20.6
          ref: 2025.2
          path: generator-1.20.6
#      - name: Checkout 1.21.4 Generator
#        uses: actions/checkout@v4
#        with:
#          repository: Spectrall368/Generator-1.21.4
#          ref: 2025.2
#          path: generator-1.21.4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21.0.7+6.0.LTS'
          distribution: 'temurin'
      - name: Cache Gradle Packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ~/.mcreator/gradle/caches/
            ~/.mcreator/gradle/wrapper/
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew
      - name: Run MCreator Test With Plugin
        run: xvfb-run -a ./gradlew test --project-prop mcreator_path=${{ github.workspace }}/mcreator
      - name: Test Javadoc Generation
        run: ./gradlew javadoc
      - name: Publish Test Report
        uses: mikepenz/action-junit-report@9379f0ccddcab154835d4e2487555ee79614fe95
        if: always() # always run even if the previous step fails
        with:
          include_passed: true
          detailed_summary: true
          report_paths: '**/build/test-results/test/TEST-*.xml'
      - name: Prepare Gradle Cache
        run: |
          rm -f  ~/.mcreator/gradle/caches/modules-2/modules-2.lock
          rm -fr ~/.mcreator/gradle/caches/*/plugin-resolution/
          rm -f  ~/.mcreator/gradle/caches/minecraft/deobfedDeps/providedDummy.jar
          rm -f  ~/.mcreator/gradle/caches/minecraft/deobfedDeps/compileDummy.jar
          rm -f  ~/.gradle/caches/modules-2/modules-2.lock
          rm -f  ~/.gradle/caches/modules-2/gc.properties